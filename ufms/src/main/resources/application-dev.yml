spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.100.91.123:3306/ufms_dev?useTimezone=true&serverTimezone=GMT%2B8
    username: ${MYSQL_NAME}
    password: ${MYSQL_PWD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    top.ordinaryroad.ufms: debug
  file:
    name: ufms_dev.log
